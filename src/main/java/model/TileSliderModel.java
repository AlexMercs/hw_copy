package model;
public class TileSliderModel {

    private int[][] board = new int[2][10];

    private void initBoard(){
        for (var row = 0; row < 2;row++){
            for (var col = 1; col < 10; col++){

            }
        }
    }
}